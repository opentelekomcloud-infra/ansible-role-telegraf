---
# tasks file for roles/executor
- name: Include Fedora variables
  include_vars: fedora.yml
  when: ansible_distribution == "Fedora"

- name: Install required packages
  become: true
  package:
    state: present
    name: "{{ item }}"
  with_items: "{{ packages }}"
  register: task_result
  until: task_result is success
  retries: 5
  delay: 2


- name: Create telegraf OS group
  become: true
  group:
    name: "{{ telegraf_os_group }}"
    state: present

- name: Create telegraf OS user
  become: true
  user:
    name: "{{ telegraf_os_user }}"
    group: "{{ telegraf_os_group }}"

- name: Make sure Telegraf Conf dir exists
  become: true
  file:
    path: "/etc/telegraf"
    state: directory
    recurse: yes
    owner: "{{ telegraf_os_user }}"
    group: "{{ telegraf_os_group }}"

    # - name: Write SSL Cert
    #   become: true
    #   copy:
    #     content: "{{ item.content }}"
    #     dest: "{{ ( '/etc/telegraf/' + item.name ) }}"
    #   when: telegraf_enable_ssl and item.content is defined
    #   loop:
    #     - {content: "{{ telegraf_ssl_cert_content }}",
    #        name: "{{ telegraf_ssl_cert_name }}"}
    #     - {content: "{{ telegraf_ssl_key_content }}",
    #        name: "{{ telegraf_ssl_key_name }}"}
    # 
    # 
    # - name: Set ssl cert permission
    #   become: true
    #   file:
    #     path: "{{ item.location }}"
    #     owner: "{{ telegraf_os_user }}"
    #     group: "{{ telegraf_os_group }}"
    #     mode: "{{ item.mode }}"
    #   when: telegraf_enable_ssl
    #   loop:
    #     - {location: "{{ telegraf_ssl_cert_location }}",
    #        mode: '0644'}
    #     - {location: "{{ telegraf_ssl_key_location }}",
    #        mode: '0600'}

- name: Write Telegraf Env file
  become: true
  template:
    src: "env"
    dest: "/etc/telegraf/env"

- name: Write Telegraf Conf
  become: true
  template:
    src: "telegraf.conf"
    dest: "/etc/telegraf/telegraf.conf"

- name: Write telegraf systemd unit file
  become: true
  template:
    dest: "{{ telegraf_systemd_unit_path }}"
    src: telegraf-service.service
  notify:
    - restart telegraf
